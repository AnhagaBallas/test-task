{
  "name": "test-task",
  "version": "1.0.0",
  "description": "test-task",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose"
  },
  "keywords": ["assignment", "testing", "vitest"],
  "license": "MIT",
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}